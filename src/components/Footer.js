import React from "react";
import { Box } from "@mui/material";

const Footer = () => {
  return (
    <Box
      sx={{
        display: "flex",
        flexDirection: "column",
        bottom: 0,
        position: "fixed",
      }}
    >
      Footer
    </Box>
  );
};

export default Footer;
